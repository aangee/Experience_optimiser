class Transform{
    /** Cree une position et une size 
     * @param {Vec2D} pos position  { x, y }
     * @param {Vec2D} size taille en { w, h } */
    constructor(pos,size) {
        this.position = pos;
        this.size = size;
    }
}