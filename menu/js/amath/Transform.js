class Transform {
    /** Cree une position et une size
     * @param {Vec2D} position position  { x, y }
     * @param {Vec2D} size taille en { w, h } */
    constructor({ position, size }) {
        this.position = position;
        this.size = size;
    }
}
