/** Permet de cree un panel simple */
class Panel extends UIElement {
    constructor(param) { super(param); }
    draw(ctx)        { super.draw(ctx); }
    drawHitArea(ctx) { super.drawHitArea(ctx); }
}
